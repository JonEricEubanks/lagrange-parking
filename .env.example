# Copy to .env and fill in. .env is gitignored.
#
# Optional — only needed if you enable the walk-time routing feature
# (enableWalkTime: true in a profile). The maps themselves are public and
# need no key, as long as the AGOL feature layers are shared publicly.
VITE_ARCGIS_API_KEY=your_api_key_here
