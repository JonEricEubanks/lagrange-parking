# Public / visitor experience (time-based parking, fully separate from permit)
VITE_PROFILE=lagrange-public
# Served at the domain root on its own Azure Static Web App.
VITE_BASE=/
VITE_OUTDIR=dist/public
